import { RunscriptsDirective } from './runscripts.directive';

describe('RunscriptsDirective', () => {
  it('should create an instance', () => {
    const directive = new RunscriptsDirective();
    expect(directive).toBeTruthy();
  });
});
